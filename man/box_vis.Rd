% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny_functions.R
\name{box_vis}
\alias{box_vis}
\title{Generate a box's visualisation}
\usage{
box_vis(var, box_type = "primary", tol = NA, match_string = NA,
  shade_label = NA, shade_relative = TRUE, df = LexOPS::lexops)
}
\arguments{
\item{var}{Character vector specifying which variable is selected}

\item{box_type}{Which type of box is it? (one of: "primary", "warning", "Success", "danger", and "info")}

\item{tol}{The area which should be highlighted. Can be a numeric vector of length two, a character vector of acceptable categories, or a list of such vectors. Each item in a \code{tol} list specifies one level.}

\item{match_string}{If the variable is being matched for, which string is it relative to. Leave as \code{NA} if not a matching box.}

\item{shade_label}{A character vector specifying what to label the levels. Order should match that of \code{tol}. Leave as \code{NA} if not doing a split.}

\item{shade_relative}{If \code{TRUE}, the value of tol is taken to be relative to the value for match_string. If \code{FALSE}, the raw values of tol are used. If \code{match_string} is not defined, \code{shade_relative} has no effect. Default is \code{TRUE}.}

\item{df}{A dataframe. Default is \code{LexOPS::lexops}.}
}
\value{
A ggplot/ggwordcloud object with the required visualisation.
}
\description{
Function for generating a visualisation for a box's selected options
}
\examples{

#box_vis("CMU.PrN", "warning", c(-1, 1), "test")

#box_vis("CMU.PrN", "warning", list(c(1, 1), c(3, 3)), shade_label = c("A1", "A2"))

#box_vis("AROU.Glasgow_Norms", "info", c(-1, 0.5), match_string = "thicket")

#box_vis("Rhyme.eSpeak.br", "danger", match_string = "thicket")

#box_vis("PoS.SUBTLEX_UK", "warning", match_string = "laura")

#box_vis("PoS.ELP", "warning")

# ignore shade_label for categorical histogram?
#box_vis("PoS.SUBTLEX_UK", "warning", list("noun", "verb"), shade_label = c("B1", "B2"))
}
